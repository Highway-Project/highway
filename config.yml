router:
  name: gorilla
  options:
    key: val
    key2: val2

services:
  - name: test-service
    loadbalancer: random
    backends:
      - name: first
        weight: 1
        address: http://localhost:9092
      - name: second
        weight: 2
        address: http://localhost:9093

rules:
  - service: test-service
    schema: http
    path-prefix: /test
    hosts:
      - localhost
    methods:
      - GET
      - POST
    headers:
      key:
        - val1
        - val2
      key2:
        - val11
        - val12
    queries:
      key: val
      key2: val2
    middlewares: []

middlewares:
  - name: custom-1
    plugin-path: /path/to/file.so
    params:
      key: val
      key1: val
  - name: middleware-2
    params:
      key: val
      key1: val